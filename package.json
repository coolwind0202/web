{
  "private": true,
  "devDependencies": {
    "prisma": "^3.12.0",
    "ts-node": "^10.7.0"
  },
  "workspaces": [
    "web"
  ],
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "scripts": {
    "dev:db_seed": "npx prisma migrate dev && npx prisma db seed"
  }
}
